<footer id="contact">
	<p class="cta">Curious to see more? Let's talk.</p>

	<p class="credit">This is just a glimpse. 12+ years of work live outside these pages.</p>

	<ul class="links">
		<li><a href="mailto:nlarooij@gmail.com">Email</a></li>
		<li><a href="https://www.linkedin.com/in/niels-larooij-61254669/" target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
		<li><a href="/resume.pdf" target="_blank" rel="noopener noreferrer">Resume</a></li>
	</ul>
</footer>

<style>
	footer {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.5rem;
		padding: 3rem 1.5rem;
		text-align: center;
		border-top: 3px solid var(--ink);
	}

	footer p {
		margin: 0;
	}

	.cta {
		font-family: var(--font-display);
		font-weight: 700;
		font-size: var(--font-size-subheading);
	}

	.links {
		display: flex;
		justify-content: center;
		gap: 0.75rem;
		list-style: none;
		margin: 1rem 0 0;
		padding: 0;
	}

	.links a {
		display: inline-block;
		font-family: var(--font-display);
		font-weight: 700;
		font-size: var(--font-size-caption);
		padding: 0.5rem 1rem;
		border: 2px solid var(--ink);
		border-radius: 999px;
		background: var(--sky);
		text-decoration: none;
		box-shadow: 3px 3px 0 var(--ink);
	}

	/* Touch-target minimum (~44px tall) on small screens. */
	@media (max-width: 640px) {
		.links a {
			padding: 0.75rem 1.25rem;
		}
	}
</style>
